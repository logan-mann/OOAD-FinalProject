package com.naiflogan.finalproject.client.shapes;

/**
 * Enum for shape types, used by ShapeCreationStrategyFactory, clientModel, etc.
 */
public enum ShapeType {
    CIRCLE,
    RECTANGLE,
    LINE
}
